function solution(start_num, end_num) {
    var answer = [];

    // answer.push(num);

    return answer;
}

// 4월 if else for while function

// 만약에 3,5 입력하였으면 배열 [3,4,5]반환
// 만약에 1,3 입력하였으면 배열 [1,2,3]반환

const result = solution(3,5);
console.log(result);